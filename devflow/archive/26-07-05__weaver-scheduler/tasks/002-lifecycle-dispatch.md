# Task 002: Runtime lifecycle and clock dispatch (PH2)

Per PLAN A2/A3/A4 + TC2/TC3/TC4: internal scheduler module under src/skein/core/weaver owning validation, clock seam, due-row selection, timer arming, dispatch. Resources live in versioned spool-state (:version + :close-fn + migrate awareness); timer arming/handler resolution happens only AFTER startup/config files finish loading (review finding: spool handlers must be resolvable); reload re-arms without double-arming or leaking timers. Dispatch rides the existing serialized event lane — no second mutation worker; saturation keeps wakes pending/re-arms preserving at-least-once. Missed-fire per TC2. Deterministic-clock tests: due dispatch, restart re-arm, reload re-arm, no-double-fire, saturation re-arm, close-on-stop thread hygiene. Own the handler INVOCATION contract explicitly: handlers are fully-qualified symbols resolved in the runtime classloader, invoked with a defined context map (document its keys in the module docstring), return values ignored, unresolvable symbols and handler exceptions captured as failed-fire records (never killing the dispatch lane) — each tested.
