# Task 003: Blessed API namespace and introspection (PH3)

Per PLAN A5/A6 + TC5: skein.api.scheduler.alpha — explicit-runtime schedule!, cancel!, and data-first introspection (pending wakes, next wake, recent fires/failures AND recent cancellations; history bounded latest-100 per category per review). No CLI surface (deferred; note rationale only if implementation makes a thin read-only op essentially free — do not build it in this task). ns docstring; accretion-compatible alpha conventions. API-level tests incl. failure shapes.
