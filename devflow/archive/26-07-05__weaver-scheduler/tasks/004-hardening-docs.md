# Task 004: End-to-end hardening, docs, spec promotion (PH4)

Per PLAN PH4: end-to-end tests in disposable weaver worlds (schedule -> fire -> handler mutates graph -> introspect; restart re-arm across a real weaver stop/start in the DISPOSABLE world only), promote the specs/*.delta.md content into devflow/specs/daemon-runtime.md and repl-api.md with section ids, document the primitive (concrete doc targets: new section in devflow/specs/daemon-runtime.md + repl-api.md from the deltas, a scheduler subsection in docs/skein.md discovery/orientation docs if weaver-owned surfaces are indexed there, and the NG6 default-guidance sentence added to docs/writing-shared-spools.md timing guidance; keep NG6 framing: pull-based wake-at + views stay the default, scheduler is the no-poller escape hatch). Add the smoke validation the review round required if not already covered. Final gate: full clojure suite + go tests + git status clean; report per-task commits.
