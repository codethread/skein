# Task 001: Scheduler persistence and validation core (PH1)

Read devflow/feat/weaver-scheduler/proposal.md, weaver-scheduler.plan.md, and specs/*.delta.md first. Implement PLAN A1: scheduler schema initialization + CRUD in skein.core.db beside strand persistence — dedicated weaver-owned tables (pending/completed/cancelled/failed, wake instant, stable key, handler symbol, JSON payload, attempt/fire/failure metadata; TC1: never strands, never visible to strand queries). Validation at the boundary: malformed schedules/handler symbols/instants fail loudly (contextual ex-info), unsupported payload values rejected not coerced. db-level tests per plan V-section: schema init idempotence, CRUD, due-row selection with injected clocks, state transitions. Own the bounded-history contract at the storage layer: completed/cancelled/failed history keeps the latest 100 per category (pruned on write), with defined orderings (pending by wake-at asc, due selection stable, history newest-first) — tested.
