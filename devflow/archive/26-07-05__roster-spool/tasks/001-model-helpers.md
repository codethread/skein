# Task 001: Roster model and explicit-runtime helpers (PH1)

Implement `spools/src/skein/spools/roster.clj` per SPEC-RosterSpool-001 P4/P5 and PLAN-RosterSpool-001 PH1 (read `devflow/feat/roster-spool/specs/roster-spool.md` and `roster-spool.plan.md` first): `roster/*` attribute vocabulary + validation/normalization, `track!`, `heartbeat!`, `finish!`, `roster` listing with stale derivation. Explicit-runtime-first (A3); ns docstring; no module-level atoms; fail loudly on malformed attrs, missing/non-roster ids, closed heartbeat targets, invalid thresholds (PH1). Tests in `test/skein/roster_test.clj` per V1/V2 using isolated weaver worlds (skein.test.alpha / spools test-support patterns; see kanban_test.clj for the shape). Validation: targeted ns green + `clojure -M:test` green.
