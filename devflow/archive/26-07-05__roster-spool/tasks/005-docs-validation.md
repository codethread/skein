# Task 005: Docs, spool contract, final validation (PH5)

Per PLAN PH5/M2: write `spools/roster.md` (contract doc matching the shipped behavior — mirror spools/kanban.md structure), add roster to `spools/README.md` index. Do NOT touch .skein config or AGENTS.md (coordinator owns canonical activation, M3). Also per PLAN M5: promote feature-spec deltas into the root specs — update devflow/specs/repl-api.md and devflow/specs/cli.md (and daemon-runtime.md if event-handler integration changes its contract) so shipped roster behavior is spec-owned, referencing SPEC-RosterSpool-001 section ids. Final gate per V6: full clojure suite + go tests + git status clean; report per-task commits in your handover. If the suite hits the catalogued parallel-load flakes (runtime-deps/chime/reap-manual), confirm solo and document.
