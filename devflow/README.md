# Devflow

Active feature work lives under `devflow/feat/`. Root specs in `devflow/specs/` become canonical only when feature work ships.

## Active features

- `agent-tool-interface` — turn the todo graph MVP into an agent-operable CLI/REPL tool.
